{% extends 'base.html.twig' %}

{% block title %}Devis index{% endblock %}

{% block body %}
    <h1>Devis index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TypeDeBien</th>
                <th>Budget</th>
                <th>Surface</th>
                <th>Description</th>
                <th>Mail</th>
                <th>Telephone</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>CodePostal</th>
                <th>Ville</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for devi in devis %}
            <tr>
                <td>{{ devi.id }}</td>
                <td>{{ devi.typeDeBien }}</td>
                <td>{{ devi.budget }}</td>
                <td>{{ devi.surface }}</td>
                <td>{{ devi.description }}</td>
                <td>{{ devi.mail }}</td>
                <td>{{ devi.telephone }}</td>
                <td>{{ devi.nom }}</td>
                <td>{{ devi.prenom }}</td>
                <td>{{ devi.codePostal }}</td>
                <td>{{ devi.ville }}</td>
                <td>
                    <a href="{{ path('devisAdmin_show', {'id': devi.id}) }}">show</a>
{#                    <a href="{{ path('devis_edit', {'id': devi.id}) }}">edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{#    <a href="{{ path('devis_new') }}">Create new</a>#}
{% endblock %}
